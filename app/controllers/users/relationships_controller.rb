class Users::RelationshipsController < ApplicationController
  # POST /users/follow
  def toggle_follow
    # if user.follow do
    #   unfollow(user)
    # else
    #   follow(user)
    # end
  end

  private

  def follow(user)
    # follow the user
  end

  def unfollow(user)
    # unfollow user
  end
end
